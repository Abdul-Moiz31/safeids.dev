{
  "name": "safeids-monorepo",
  "private": true,
  "packageManager": "pnpm@10.34.1",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
